# CLASSHUB V1 — FIXED

Upload these files to the root of your GitHub Pages repository:

- index.html
- style.css
- app.js

GitHub Pages should then load the JavaScript correctly. The script reference uses cache-busting (`?v=2`) so an older cached app.js is less likely to remain active.
